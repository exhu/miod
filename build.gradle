subprojects {
    apply plugin: 'java'
    repositories {
        flatDir {
            dirs "$rootDir/javalibs"
        }
        mavenCentral()
        //maven {
        //    url "http://repo1.maven.org/maven2"
        //}
    }
}
